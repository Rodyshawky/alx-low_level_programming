#include"main.h"

/**
 * _isupper -check uppercase character
 * @c: input character
 * Return: On success 1 if not return 0.
 */

int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
else
return (0);
}
