#include"main.h"

/**
 * _strlen -get the length of a string.
 * @s: input pointer
 * Return: return the length of a string..
 */

int _strlen(char *s)
{
int i;
int x;

i = sizeof(s) / sizeof (s[0]);
x = i;
return (x);
}
